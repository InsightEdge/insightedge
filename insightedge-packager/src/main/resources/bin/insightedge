#!/bin/bash
DIRNAME=$(dirname ${BASH_SOURCE[0]})
source ${DIRNAME}/../insightedge/conf/insightedge-env.sh

INSIGHTEDGE_CLI_CP=${XAP_HOME}/tools/cli/*:${INSIGHTEDGE_CLASSPATH}:${SIGAR_JARS}
"${JAVACMD}" ${JAVA_OPTIONS} ${XAP_OPTIONS} ${XAP_CLI_OPTIONS} -cp ${INSIGHTEDGE_CLI_CP} org.insightedge.cli.commands.I9EMainCommand $*
