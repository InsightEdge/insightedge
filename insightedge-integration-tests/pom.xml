<?xml version="1.0" encoding="UTF-8"?>

<project>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>insightedge-integration-tests</artifactId>
    <description>InsightEdge Integration Tests</description>

    <parent>
        <groupId>org.gigaspaces.insightedge</groupId>
        <artifactId>insightedge-package</artifactId>
        <version>1.1.0-SNAPSHOT</version>
    </parent>

    <repositories>
        <repository>
            <id>org.openspaces</id>
            <name>OpenSpaces</name>
            <url>http://maven-repository.openspaces.org</url>
            <releases>
                <enabled>true</enabled>
                <updatePolicy>daily</updatePolicy>
                <checksumPolicy>warn</checksumPolicy>
            </releases>
            <snapshots>
                <enabled>true</enabled>
                <updatePolicy>always</updatePolicy>
                <checksumPolicy>warn</checksumPolicy>
            </snapshots>
        </repository>
    </repositories>

    <properties>
        <dist.edition></dist.edition>
        <dist.dir>${project.basedir}/../insightedge-packager/target/${dist.edition}</dist.dir>
        <git.branch></git.branch>
        <gsVersion>12.0.1</gsVersion>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.gigaspaces</groupId>
            <artifactId>xap-asm</artifactId>
            <version>${gsVersion}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.gigaspaces</groupId>
            <artifactId>xap-datagrid</artifactId>
            <version>${gsVersion}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.gigaspaces</groupId>
            <artifactId>xap-openspaces</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>com.gigaspaces</groupId>
            <artifactId>xap-admin</artifactId>
            <version>${gsVersion}</version>
            <scope>compile</scope>
        </dependency>


        <dependency>
            <groupId>org.gigaspaces.insightedge</groupId>
            <artifactId>insightedge-scala</artifactId>
        </dependency>
        <!-- Scala -->
        <dependency>
            <groupId>org.scala-lang</groupId>
            <artifactId>scala-library</artifactId>
        </dependency>
        <dependency>
            <groupId>org.scala-lang</groupId>
            <artifactId>scala-reflect</artifactId>
        </dependency>
        <dependency>
            <groupId>org.scala-lang</groupId>
            <artifactId>scala-compiler</artifactId>
        </dependency>

        <dependency>
            <groupId>com.gigaspaces</groupId>
            <artifactId>xap-build-plugin</artifactId>
            <version>12.0.1-SNAPSHOT</version>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.scalatest</groupId>
            <artifactId>scalatest_2.10</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.scala-lang</groupId>
                    <artifactId>scala-library</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>com.spotify</groupId>
            <artifactId>docker-client</artifactId>
            <version>6.1.0</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-log4j12</artifactId>
            <version>1.7.21</version>
            <scope>test</scope>
        </dependency>
        <!-- REST client lib -->
        <dependency>
            <groupId>com.typesafe.play</groupId>
            <artifactId>play-ws_2.10</artifactId>
            <version>2.4.6</version>
        </dependency>
    </dependencies>

    <!-- Integration tests execution are turned off by default profile -->
    <build>
        <plugins>
            <!-- Directory plugin to find parent root directory absolute path -->
            <plugin>
                <groupId>org.commonjava.maven.plugins</groupId>
                <artifactId>directory-maven-plugin</artifactId>
                <version>0.1</version>
                <executions>
                    <execution>
                        <id>directories</id>
                        <goals>
                            <goal>highest-basedir</goal>
                        </goals>
                        <phase>initialize</phase>
                        <configuration>
                            <property>main.basedir</property>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- This plugin builds a Docker image -->
            <plugin>
                <groupId>com.spotify</groupId>
                <artifactId>docker-maven-plugin</artifactId>
                <version>0.4.9</version>
                <executions>
                    <execution>
                        <id>build-failover-image</id>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>build</goal>
                        </goals>
                        <configuration>
                            <imageName>insightedge-test:${project.version}</imageName>
                            <dockerDirectory>
                                ${main.basedir}/insightedge-integration-tests/src/test/resources/docker/failover
                            </dockerDirectory>
                            <resources>
                                <resource>
                                    <targetPath>/</targetPath>
                                    <directory>${dist.dir}</directory>
                                    <include>*.zip</include>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                    <execution>
                        <id>build-demo-mode-image</id>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>build</goal>
                        </goals>
                        <configuration>
                            <imageName>insightedge-tests-demo-mode:${project.version}</imageName>
                            <dockerDirectory>
                                ${main.basedir}/insightedge-integration-tests/src/test/resources/docker/demo-mode
                            </dockerDirectory>
                            <resources>
                                <resource>
                                    <targetPath>/</targetPath>
                                    <directory>${dist.dir}</directory>
                                    <include>*.zip</include>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                    <execution>
                        <id>build-cluster-install-image</id>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>build</goal>
                        </goals>
                        <configuration>
                            <imageName>insightedge-tests-cluster-install:${project.version}</imageName>
                            <dockerDirectory>
                                ${main.basedir}/insightedge-integration-tests/src/test/resources/docker/cluster-install
                            </dockerDirectory>
                            <resources>
                                <resource>
                                    <targetPath>/</targetPath>
                                    <directory>
                                        ${main.basedir}/insightedge-integration-tests/src/test/resources/docker/cluster-install
                                    </directory>
                                    <!--<include>ie-user.pem</include>-->
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                    <execution>
                        <id>build-maven-install-libs-image</id>
                        <phase>none</phase>
                        <goals>
                            <goal>build</goal>
                        </goals>
                        <configuration>
                            <imageName>insightedge-tests-maven-install-libs:${project.version}</imageName>
                            <dockerDirectory>
                                ${main.basedir}/insightedge-integration-tests/src/test/resources/docker/maven-install-libs
                            </dockerDirectory>
                            <resources>
                                <resource>
                                    <targetPath>/</targetPath>
                                    <directory>
                                        ${dist.dir}
                                    </directory>
                                    <include>*.zip</include>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>net.alchim31.maven</groupId>
                <artifactId>scala-maven-plugin</artifactId>
                <version>3.2.1</version>
                <executions>
                    <execution>
                        <id>scala-compile-first</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>add-source</goal>
                            <goal>compile</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>scala-test-compile</id>
                        <phase>process-test-resources</phase>
                        <goals>
                            <goal>testCompile</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.scalatest</groupId>
                <artifactId>scalatest-maven-plugin</artifactId>
                <version>1.0</version>
                <executions>
                    <execution>
                        <id>test</id>
                        <goals>
                            <goal>test</goal>
                        </goals>
                        <phase>integration-test</phase>
                    </execution>
                </executions>
                <configuration>
                    <argLine>-Ddist.edition=${dist.edition} -Ddist.version=${project.version} -Ddist.dir=${dist.dir}</argLine>
                    <argLine>-Ddist.edition=${dist.edition} -Ddist.version=${project.version} -Dgit.branch=${git.branch}</argLine>
                    <reportsDirectory>${project.build.directory}/surefire-reports</reportsDirectory>
                    <junitxml>.</junitxml>
                    <filereports>scalatest.txt</filereports>
                    <tagsToExclude>org.insightedge.spark.utils.LongRunningTest</tagsToExclude>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <!--don't change the profile names, newman relies on it!-->
    <profiles>
        <profile>
            <id>run-integration-tests-community</id>
            <properties>
                <dist.edition>community</dist.edition>
            </properties>
        </profile>

        <profile>
            <id>run-integration-tests-premium</id>
            <properties>
                <dist.edition>premium</dist.edition>
            </properties>
        </profile>

        <!-- when running integration tests against external already built dist -->
        <profile>
            <id>run-external</id>
            <dependencies>
                <dependency>
                    <groupId>org.gigaspaces.insightedge</groupId>
                    <artifactId>insightedge-scala</artifactId>
                    <scope>system</scope>
                    <systemPath>${dist.dir}/lib/insightedge-scala-${project.version}.jar</systemPath>
                </dependency>
            </dependencies>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.scalatest</groupId>
                        <artifactId>scalatest-maven-plugin</artifactId>
                        <configuration>
                            <tagsToExclude>_</tagsToExclude> <!-- Workaround to reset previous value-->
                            <argLine>-Ddist.edition=${dist.edition} -Ddist.version=${project.version} -Ddist.dir=${dist.dir} -Dgit.branch=${git.branch}</argLine>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>com.spotify</groupId>
                        <artifactId>docker-maven-plugin</artifactId>
                        <executions>
                            <!--Don't build images, only run tests-->
                            <execution>
                                <id>build-maven-install-libs-image</id>
                                <phase>none</phase>
                            </execution>
                            <execution>
                                <id>build-demo-mode-image</id>
                                <phase>none</phase>
                            </execution>
                            <execution>
                                <id>build-cluster-install-image</id>
                                <phase>none</phase>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>setup-external</id>
            <dependencies>
                <dependency>
                    <groupId>org.gigaspaces.insightedge</groupId>
                    <artifactId>insightedge-scala</artifactId>
                    <scope>system</scope>
                    <systemPath>${dist.dir}/lib/insightedge-scala-${project.version}.jar</systemPath>
                </dependency>
            </dependencies>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.scalatest</groupId>
                        <artifactId>scalatest-maven-plugin</artifactId>
                        <configuration>
                            <argLine>-Ddist.edition=${dist.edition} -Ddist.version=${project.version} -Ddist.dir=${dist.dir} -Dgit.branch=${git.branch}</argLine>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>com.spotify</groupId>
                        <artifactId>docker-maven-plugin</artifactId>
                        <executions>
                            <!--Images to build-->
                            <execution>
                                <id>build-maven-install-libs-image</id>
                                <phase>pre-integration-test</phase>
                            </execution>
                            <execution>
                                <id>build-demo-mode-image</id>
                                <phase>pre-integration-test</phase>
                            </execution>
                            <execution>
                                <id>build-cluster-install-image</id>
                                <phase>pre-integration-test</phase>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>only-long-running-test</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.scalatest</groupId>
                        <artifactId>scalatest-maven-plugin</artifactId>
                        <configuration>
                            <tagsToExclude>_</tagsToExclude> <!-- Workaround to reset previous value-->
                            <tagsToInclude>org.insightedge.spark.utils.LongRunningTest</tagsToInclude>
                            <argLine>-Ddist.edition=${dist.edition} -Ddist.version=${project.version} -Ddist.dir=${dist.dir}</argLine>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>com.spotify</groupId>
                        <artifactId>docker-maven-plugin</artifactId>
                        <executions>
                            <!--Images to build-->
                            <execution>
                                <id>build-maven-install-libs-image</id>
                                <phase>pre-integration-test</phase>
                            </execution>

                            <!--Images to exclude -->
                            <execution>
                                <id>build-demo-mode-image</id>
                                <phase>none</phase>
                            </execution>
                            <execution>
                                <id>build-cluster-install-image</id>
                                <phase>none</phase>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Turns off execution if no profile is specified -->
        <profile>
            <id>run-nothing</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>org.gigaspaces.insightedge</groupId>
                    <artifactId>insightedge-packager</artifactId>
                    <version>${project.version}</version>
                </dependency>
            </dependencies>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.spotify</groupId>
                        <artifactId>docker-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>build-demo-mode-image</id>
                                <phase>none</phase>
                            </execution>
                            <execution>
                                <id>build-cluster-install-image</id>
                                <phase>none</phase>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.scalatest</groupId>
                        <artifactId>scalatest-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>test</id>
                                <phase>none</phase>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>com.gigaspaces</groupId>
                        <artifactId>xap-build-plugin</artifactId>
                        <version>12.0.1-SNAPSHOT</version>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>generateByScanner</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <type>insightedge-integration</type>
                            <outputFile>${project.basedir}/../insightedge-packager/target/ie-integration-tests.json</outputFile>
                            <scanner>com.gigaspaces.newman.testgenerator.scanner.InsightEdgeIntegrationTestsScanner</scanner>
                            <includeTests>true</includeTests>
                            <includeSources>true</includeSources>
                            <packages>
                                <package>org.insightedge.spark</package>
                            </packages>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
